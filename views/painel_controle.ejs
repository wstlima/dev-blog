<% include ./_painel_topo.ejs%>
        <div class="jumbotron">
            <h1>Bem Vindo ao painel do <%= user.acesso%></h1>
        </div>
        <%if(user.acesso == 'admin'){%>
        <table class="table table-striped table-hover table-bordered">
            <thead>
            <tr>
                <th>#</th>
                <th>Nome</th>
                <th>Usuário</th>
                <th>Acesso</th>
                <th>Sobre</th>
                <th>Postagens</th>
                <th>Editar</th>
            </tr>
            </thead>
            <tbody>
                <%for(var u of users){%>
                <tr>
                    <td></td>
                    <td><%= u.nome %></td>
                    <td><%= u.usuario %></td>
                    <td><%= u.acesso %></td>
                    <td><%= u.sobre %></td>
                    <td><%= 0 %></td>
                    <td><a class="btn btn-primary" href="controlpanel/users/edit/<%= u.usuario %>">Editar</a></td>
                </tr>
            <%}%>
            </tbody>
        </table>
        <%}%>
<% include ./_painel_rodape.ejs%>